1. page 0 contains the init program's text/data. page 2 is the user stack. page 1 is the stack guard page; user mode cannot access it because PTE_U is cleared.

2. The srcva + len < srcva check catches unsigned overflow. For example, srcva = 0xfffffffffffffff0 and len = 0x20 makes srcva + len wrap around to 0x10, so the first two size checks can be fooled without the overflow check.
